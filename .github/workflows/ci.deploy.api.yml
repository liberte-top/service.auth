name: ci.deploy.api

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - "api/**"
      - "docker-compose.yml"
      - ".github/workflows/ci.deploy.api.yml"
  pull_request:
    paths:
      - "api/**"
      - "docker-compose.yml"
      - ".github/workflows/ci.deploy.api.yml"

jobs:
  build-test-api:
    runs-on: linux-x64-8c
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: api -> target

      - name: Run tests
        working-directory: api
        run: cargo test --locked

      - name: Capture build timestamp
        id: build_meta
        run: echo "timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build API image
        uses: docker/build-push-action@v7
        with:
          context: ./api
          push: ${{ github.event_name != 'pull_request' }}
          build-args: |
            APP_BUILD_SHA=${{ github.sha }}
            APP_BUILD_TIMESTAMP=${{ steps.build_meta.outputs.timestamp }}
          cache-from: type=gha,scope=service-auth-api
          cache-to: type=gha,mode=max,scope=service-auth-api
          tags: |
            ghcr.io/liberte-top/service-auth-api:main
            ghcr.io/liberte-top/service-auth-api:sha-${{ github.sha }}

      - name: Checkout kubernetes repo
        if: github.event_name != 'pull_request'
        uses: actions/checkout@v5
        with:
          repository: liberte-top/kubernetes
          ref: main
          token: ${{ secrets.KUBERNETES_REPO_TOKEN }}
          path: kubernetes

      - name: Update auth-api image tag
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          path = Path('kubernetes/charts/service/auth/values.yaml')
          replacement = '    tag: sha-${{ github.sha }} # auth-api-image'
          lines = []
          for line in path.read_text().splitlines():
            if line.strip().endswith('# auth-api-image'):
              line = replacement
            lines.append(line)
          path.write_text('\n'.join(lines) + '\n')
          PY
      - name: Create kubernetes promotion PR
        if: github.event_name != 'pull_request'
        id: create_pr
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.KUBERNETES_REPO_TOKEN }}
          path: kubernetes
          branch: promote/auth-api-${{ github.sha }}
          delete-branch: true
          commit-message: "chore: promote auth-api image"
          title: "chore: promote auth-api image"
          body: |
            Update `auth-api` to `ghcr.io/liberte-top/service-auth-api:sha-${{ github.sha }}`.

      - name: Enable PR auto-merge
        if: github.event_name != 'pull_request' && steps.create_pr.outputs.pull-request-number != ''
        env:
          GH_TOKEN: ${{ secrets.KUBERNETES_REPO_TOKEN }}
        run: |
          set -euo pipefail
          pr_number="${{ steps.create_pr.outputs.pull-request-number }}"
          gh pr merge --repo "liberte-top/kubernetes" --squash "$pr_number" || {
            gh pr update-branch --repo "liberte-top/kubernetes" "$pr_number" --rebase
            gh pr merge --repo "liberte-top/kubernetes" --squash "$pr_number"
          }
